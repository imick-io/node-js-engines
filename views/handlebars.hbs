
<h1>This is a title</h1>

<p>Let's create a loop</p>
<ul>
    {{#each items}}
        <li>{{this.title}}</li>
    {{/each}}
</ul>

<div>
    <p>Let's create a if</p>
    {{#if name}}
        <p>{{name}}</p>
    {{else}}
        <p>No name... 🌶️</p>
    {{/if}}
</div>
